name: Lint and test Cairo
# This workflow runs linting and tests on all pull requests and pushes to the main branch.
# It includes Markdown linting, Cairo formatting checks, running tests with coverage,
# and uploading the coverage report to Codecov.

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  lint_and_test:
    name: Lint and test Cairo
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Extract scarb version
        run: |
          SCARB_VERSION=$(grep 'scarb-version = ' Scarb.toml | sed 's/scarb-version = "\(.*\)"/\1/')
          echo "SCARB_VERSION=$SCARB_VERSION" >> "$GITHUB_ENV"

      - uses: software-mansion/setup-scarb@v1
        with:
          scarb-version: ${{ env.SCARB_VERSION }}

      # - name: Extract foundry version
      #   run: |
      #     FOUNDRY_VERSION=$(grep 'snforge_std = ' Scarb.toml | sed 's/snforge_std = "\(.*\)"/\1/')
      #     echo "FOUNDRY_VERSION=$FOUNDRY_VERSION" >> "$GITHUB_ENV"

      # - uses: foundry-rs/setup-snfoundry@v5.0.0
      #   with:
      #     starknet-foundry-version: ${{ env.FOUNDRY_VERSION }}
      #
      - uses: dtolnay/rust-toolchain@stable
      - name: Clone foundry
        run: |
          git clone https://github.com/maciektr/starknet-foundry.git
          cd starknet-foundry
          git checkout dd6fcb815ff9a1e226cfcf48febaae4378f689a3
          cd ..
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "starknet-foundry -> target"

      - name: Install USC
        run: curl -L https://raw.githubusercontent.com/software-mansion/universal-sierra-compiler/master/scripts/install.sh | sh

      - name: Compile foundry
        run: |
          cd starknet-foundry
          cargo build --release -p forge
          cd ..

      - name: Install cairo-coverage
        run: curl -L https://raw.githubusercontent.com/software-mansion/cairo-coverage/main/scripts/install.sh | sh

      # - name: Markdown lint
      #   uses: DavidAnson/markdownlint-cli2-action@07035fd053f7be764496c0f8d8f9f41f98305101 # v16
      #   with:
      #     globs: |
      #       *.md
      #       !PULL_REQUEST_TEMPLATE.md

      # - name: Cairo lint
      #   run: scarb fmt --check --workspace

      # - name: Run tests
      #   run: ./starknet-foundry/target/release/snforge test --workspace --features fuzzing --fuzzer-runs 200

      # - name: Run tests and generate coverage report
      #   run: ./starknet-foundry/target/release/snforge test --workspace --coverage

      # - name: Upload coverage to Codecov
      #   uses: codecov/codecov-action@v4
      #   with:
      #     file: ./coverage.lcov
      #     token: ${{ secrets.CODECOV_TOKEN }}
